<div class="three columns itens" onclick="spotcard.companies({{category_id}}, function(response) {console.log(response);});" >
    <a href="#secondPage" class="link_category">
        <figure class="foto-legenda"> 
            <img src="{{img}}" alt="texto alternativo" />
                <figcaption><h3 class="red_font">{{name}}</h3></figcaption>
        </figure>
    </a>
</div>